# Kubernetes Manifests

This directory contains Kubernetes manifests for platform components and ArgoCD applications.

## Structure

- `platform/` - Platform component configurations (ArgoCD, Ingress, cert-manager, ExternalDNS)
- `argocd-apps/` - ArgoCD Application definitions for GitOps management
